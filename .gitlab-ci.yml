# ====================================================================
# 메인 CI/CD 설정 파일 (루트 경로 버전)
# ====================================================================

stages:
  - build
  - deploy

include:
  # ci/ 폴더 경로를 제거하고 파일 이름만 남깁니다.
  - "gitlab-ci-frontend.yml"
  - "gitlab-ci-backend.yml"
