<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.nareugobackend.mapper.UserMapper">

    <select id="findByEmail" parameterType="string" resultType="org.example.nareugobackend.domain.user.User">
        SELECT id, email, name, gender, birth, phone_number, provider_type, provider_id, created_at, updated_at
        FROM users
        WHERE email = #{email}
    </select>

    <select id="existsByEmail" parameterType="string" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM users
        WHERE email = #{email}
    </select>

</mapper>