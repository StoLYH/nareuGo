<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.nareugobackend.mapper.OrderMapper">
  <!-- 주문(Order) 관련 MyBatis 매퍼 XML -->
  <!-- 현재 사용하지 않아 주석 처리 -->
  
  <!--
  <select id="findById" resultType="org.example.nareugobackend.common.model.Order">
    SELECT order_id, product_id, buyer_id, status, amount, created_at, updated_at
    FROM orders
    WHERE order_id = #{orderId}
  </select>
  
  <select id="findByProductId" resultType="org.example.nareugobackend.common.model.Order">
    SELECT order_id, product_id, buyer_id, status, amount, created_at, updated_at
    FROM orders
    WHERE product_id = #{productId}
    ORDER BY order_id DESC
    LIMIT 1
  </select>
  
  <update id="updateStatus">
    UPDATE orders
    SET status     = #{status},
        updated_at = NOW()
    WHERE order_id = #{orderId}
  </update>
  
  <insert id="insert" parameterType="org.example.nareugobackend.common.model.Order" useGeneratedKeys="true" keyProperty="orderId">
    INSERT INTO orders (product_id, buyer_id, status, amount)
    VALUES (#{productId}, #{buyerId}, #{status}, #{amount})
  </insert>
  
  <update id="repend">
    UPDATE orders
    SET status = 'PAYMENT_PENDING', buyer_id = #{buyerId}, amount = #{amount}, updated_at = NOW()
    WHERE order_id = #{orderId}
  </update>
  
  <select id="findExpiredPendingOrders" resultType="org.example.nareugobackend.common.model.Order">
    SELECT order_id, product_id, buyer_id, status, amount, created_at, updated_at
    FROM orders
    WHERE status = 'PAYMENT_PENDING'
      AND created_at < DATE_SUB(NOW(), INTERVAL 15 MINUTE)
  </select>
  
  <update id="expireOrders">
    UPDATE orders
    SET status = 'CANCELLED', updated_at = NOW()
    WHERE order_id IN
    <foreach collection="orderIds" item="orderId" open="(" separator="," close=")">
      #{orderId}
    </foreach>
  </update>
  -->
</mapper>