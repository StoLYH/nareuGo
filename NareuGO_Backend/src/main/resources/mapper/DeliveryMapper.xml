<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.nareugobackend.mapper.DeliveryMapper">

  <insert id="insertInitialDelivery">
    INSERT INTO deliveries (
      order_id,
      delivery_address,
      status,
      tracking_number
    ) VALUES (
      #{orderId},
      #{deliveryAddress},
      #{status},
      #{trackingNumber}
    )
  </insert>

</mapper>
